using System;
using System.Collections.Generic;
using System.Text;

namespace Algorithms_Assignment
{
    class BinarySearch
    {
        //DOES NOT WORK, NOT IMPLEMENTED PROPERLY OUTSIDE CLASS!
        //public int Binary(int[] SortedArray, int key)
        //{
        //    int steps = 0;
        //    int leftVal = 0;
        //    int rightVal = SortedArray.Length - 1;
        //    while(leftVal <= rightVal)
        //    {
        //       int midVal = 1 + (rightVal - leftVal) / 2;
        //        if (SortedArray[midVal] == key)
        //        {
        //            steps++;
        //            return midVal;
        //        }
        //        else if (SortedArray[midVal] < key)
        //        {
        //            steps++;
        //            leftVal = midVal + 1;
        //        }
        //       else
        //            steps++;
        //            rightVal = midVal + 1;
        //    }
        //    Console.WriteLine("index position of value within sorted array at: " + key);
        //    Console.WriteLine("number of steps taken to complete operation is: " + steps);
        //    return -1;
        //}
    }
}
